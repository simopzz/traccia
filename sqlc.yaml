version: "2"
sql:
  - engine: "postgresql"
    schema: "migrations"
    queries: "internal/repository/sql"
    gen:
      go:
        package: "repository"
        out: "internal/repository"
        sql_package: "pgx/v5"
        emit_json_tags: false
        emit_empty_slices: true
